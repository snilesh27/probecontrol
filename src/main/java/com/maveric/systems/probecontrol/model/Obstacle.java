package com.maveric.systems.probecontrol.model;

// Obstacle class represent actual blokced positions for moving with x,y co ordinates
public class Obstacle extends Position {
    public Obstacle(int x, int y) {
        super(x, y);
    }
}
